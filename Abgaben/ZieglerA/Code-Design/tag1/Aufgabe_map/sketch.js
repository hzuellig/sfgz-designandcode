function setup() {
    // put setup code here
    createCanvas(windowWidth, windowHeight);
    background(230, 250, 30);
}

function draw() {
    // put drawing code here

    triangle(0, windowWidth, windowHeight, 0);

    triangle(windowWidth, 0, windowHeight, 0);

    triangle(windowHeight, 00, 0, windowWidth);

    triangle(windowHeight, 0, windowWidth, 0);



}